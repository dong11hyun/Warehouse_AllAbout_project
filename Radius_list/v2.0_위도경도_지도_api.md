# 🏪 다이소 상권 분석 & 지도 시각화 (Daiso Radius Map)

> **카카오맵 API**를 활용하여 전국 다이소 지점 주변의 상권을 분석하고, 수집된 데이터를 **웹 지도(Web Map)** 상에 시각화하여 직관적인 인사이트를 함

---

## 🚀 주요 업데이트 (Major Updates)

### 1. 🗺️ 대화형 지도 시각화 `http://127.0.0.1:8000/map/`

텍스트로만 확인하던 상권 데이터를 **실제 지도 화면**에서 확인할 수 있습니다.

- **웹 뷰어 제공:** `/map/` 경로를 통해 수집된 모든 매장의 위치를 한눈에 파악

- **마커(Marker) 표시:** 다이소 지점 주변의 편의점, 카페, 마트 위치에 핀 고정

- **인포윈도우:** 마커 클릭 시 해당 매장의 상호명 등 상세 정보 표시

- **카카오 지도 SDK 연동:** 줌 인/아웃, 지도 이동 등 부드러운 UX 제공

### 2. 📍 정밀 위치 데이터 확보 (Lat/Lng Support)
단순 주소 수집을 넘어, 지리 정보 시스템(GIS) 분석이 가능한 좌표 데이터를 확보했습니다.

- **DB 스키마 확장:** `NearbyStore` 모델에 위도(`lat`), 경도(`lng`) 필드 추가

- **데이터 파이프라인 고도화:** 데이터 수집 시 Kakao API의 `x`, `y` 좌표 값을 자동으로 DB에 적재

---

## 📊 데이터베이스 구조 (Database Model)

지도 시각화를 위해 **좌표 필드(Lat, Lng)**가 추가된 최신 스키마입니다.

| Field Name | Type | Description | Note |
|---|---|---|---|
| `base_daiso` | Char | 기준 다이소 지점명 | (FK 개념) |
| `name` | Char | 매장 상호명 | |
| `address` | Char | 도로명 주소 | |
| `distance` | Integer | 다이소와의 거리 (m) | |
| **`lat`** | **Float** | **위도 (Latitude, y)** | |
| **`lng`** | **Float** | **경도 (Longitude, x)** | |
| `created_at` | DateTime | 수집 일시 | |

